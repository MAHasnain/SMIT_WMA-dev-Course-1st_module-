// ✅ 3. Prompt-Based Questions (User Input)
// Take user input for name and greet them with an alert.

var userName = prompt("Please enter your name");
var greetMsg = `Salam ${userName} `;
// alert(greetMsg)
console.log(greetMsg);

// Ask the user for age and display a message: “You are eligible to vote” or not.

var userAge = +prompt("Enter your age");
if (userAge >= 18) {
    alert("You are eligible to vote");
} else {
    alert("You are'nt eligible to vote");
};

// Prompt for 2 numbers and show their sum.


// Take user input in Celsius and convert it to Fahrenheit.

// Ask the user their city and display a welcome message.

// Prompt for 3 subject marks and calculate total and percentage.

// Get the user’s favorite color and change the background color using JS.

// Take two numbers, subtract the smaller from the larger, and show the result.

// Prompt the user to enter a year and check if it’s a leap year.

// Ask the user’s birth year and calculate their age.

// Take the base and height and calculate the area of a triangle.

// Take salary and calculate 10% tax.

// Prompt for a product price and quantity and show total bill.

// Prompt for a number and use Math.sqrt to find the square root.

// Take name, age, and city and show all in a formatted alert box.

// Ask for radius and calculate area of a circle.

// Get two numbers and show which one is greater.

// Prompt for 5 numbers and show their average.

// Take marks out of 100 and convert to grade using if-else.

// Use prompt to ask for user’s hobby and display a fun message.

