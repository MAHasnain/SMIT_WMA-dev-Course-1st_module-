// ✅ 4. If-Else & Else-If Statements (with Comparison Operators) – 20 Code-Based Questions
// Write a program to check if a number is positive or negative.

var isPosOrNeg = 20;
if (isPosOrNeg >= 0) {
    console.log("Number is positive " + isPosOrNeg);
} else {
    console.log("Number is negative " + isPosOrNeg);
}

// Ask user for age and display “Child”, “Teenager”, or “Adult” based on value.

// var userAge = prompt("Enter your age");
var userAge = 24;
if (userAge <= 10) {
    console.log("You're child.");
} else if (userAge <= 20) {
    console.log("You're Teenager.");
} else if (userAge <= 30) {
    console.log("You're Adult.");
} else {
    console.log("You're old Age");
}

// Take a number and check if it’s divisible by 5.

var num1 = 10
if (num1 % 5 == 0) {
    console.log("Number is divisible by 5");
} else {
    console.log("Number is'nt divisible by 5");
}

// Write a program to check whether a number is even or odd using if-else.

// Check if a user is eligible for driving (age ≥ 18).

// Prompt user to enter marks and display: A, B, C, or Fail using else if.

// Compare two numbers and print the larger one.

// Take a temperature and suggest: “Hot”, “Moderate”, or “Cold”.

// Ask for gender and display different greetings for “male” and “female”.

// Check if a number lies between 10 and 50.

// Check if user input is exactly equal to the string "Hello".

// Compare two strings using === and print match or not.

// Check if the entered year is a leap year using if-else.

// Prompt for a password and check if it matches the saved one.

// Write a grading system based on percentage using multiple else if.

// Create a login system with hardcoded username and password.

// Check if a number is a multiple of 3 and 7.

// Ask the user for current time (24-hour format) and suggest "Good morning", "Good evening", etc.

// Use logical operators to check if a number is between 20 and 40 AND even.

// Check if the input number is a prime number (basic logic with if-else).