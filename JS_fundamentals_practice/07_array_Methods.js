// 🔹 Basic Array Operations (Q1–10)
// Declare an array of 5 fruits and display it in the console.

// Access the 3rd element of an array and print it.

// Change the 2nd value in an array and display the updated array.

// Find the length of an array and log it.

// Create an array of 5 numbers and print the first and last element.

// Loop through an array and print each element.

// Check if a specific value exists in the array using includes().

// Add a new element at the end of an array using array[array.length].

// Create an array with mixed types (number, string, boolean).

// Create an empty array and push 5 values into it using a loop.



// 🔹 Array Methods Practice (Q11–24)
// Use .push() to add "banana" and "grape" to an array of fruits.

// Use .pop() to remove the last element of an array and print the updated array.

// Use .shift() to remove the first element and log the result.

// Use .unshift() to add "apple" to the start of an array.

// Use .sort() to sort an array of numbers in ascending order.

// Use .sort() to sort an array of strings alphabetically.

// Use .join() to convert an array into a single string separated by commas.

// Combine two arrays using .concat() and log the result.

// Reverse the elements of an array using .reverse().

// Use .indexOf() to find the index of a specific element.

// Remove the last 2 elements using .pop() twice.

// Add three items to the start using .unshift() in a single line.

// Create an array of marks and check if any value is above 90 using .some().

// Create an array of names and check if all start with “A” using .every().


// 🔹 Advanced Array Challenges (Q25–30)
// Write a program to count how many times “apple” appears in an array.

// Create an array of numbers and filter out even numbers into a new array.

// Find the maximum and minimum number in an array (without using Math).

// Replace a specific value in an array (e.g., replace “cat” with “lion”).

// Take 5 inputs from the user using prompt and store them in an array.

// Create a shopping cart array and remove a specific item if it exists.